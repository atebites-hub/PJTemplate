# Environment Variables for [Project Name]
# Copy to .env and fill in values. Never commit .env.

# API Keys
API_KEY=[your-api-key-here]  # E.g., OpenAI or Holochain
DATABASE_URL=[your-db-url]   # E.g., postgres://...

# Network Config
NETWORK=[devnet|mainnet]     # E.g., Solana network
PORT=3000                    # Dev server port

# Auth/Security
SECRET_KEY=[random-secret]   # For JWT or hashing

# Custom (e.g., for games)
GAME_SCALE=4.85              # Rendering scale factor

Assumptions: Use for secrets (gitignore .env). Known Issues: Expose only what's needed; validate in setup.sh.