# Documentation

Welcome. Agent process templates live under **Agent templates** in the nav.

Optional **dev notebooks** for data exploration are documented under **Test docs →
Dev notebooks** (`docs/tests/notebooks.md`). Install with
`./scripts/setup.sh --with-notebooks`.

Run locally:

```bash
mkdocs serve -f config/docs/mkdocs.yml
```
